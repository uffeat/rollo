# About

Anvil does not support wild-card routing, so the router builds routes by factory to absorb. 

The router handles page routing, but more fine-grained use of routes if left to client-side.

Supports routing to client_code as well as to theme/assets.