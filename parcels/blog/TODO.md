# Placeholders and progressive images
Currently, not critical, but could be a nice touch.

# Pagination
Limiting card view to say 8 items and then import render content at each page change would almost certainly guarantee a smooth first page-load UX. BUt this would require:
- Pagination logic
- Build tool integration, so that chucks corresponding to a single page could be imported one at the time.
