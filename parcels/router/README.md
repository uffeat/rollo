# About

History-based browser router that does not require route registration.

Supports for nested routes. 


# How it works



# Nav updates

Effecs can be registered on the router to enable management of active links.









/* Expose current path as app attr/reactive item; can be used for 
      advanced dynamic styling */
      app.$({ path });
      /* Provide service, e.g. to manage active state in nav groups. */